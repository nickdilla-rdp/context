Поместите сюда текстуры если нужны:
  - texture_dark.png
  - texture_light.png

Если текстуры не используются — функции load_texture() вернут ошибку,
которая обрабатывается в JS тихо (if/else guard).
